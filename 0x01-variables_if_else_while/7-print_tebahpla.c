#include <stdio.h>
/**
 * main - Start of prog
 * Return: 0 (Perfect)
 */

int main(void)
{
	char reverse;

	reverse = 'z';
	while
		(reverse >= 'a')
		{
			putchar(reverse);
			reverse--;

		}
	putchar ('\n');
	return (0);
}
