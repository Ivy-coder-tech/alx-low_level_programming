#include <stdio.h>
/**
 * main - Allows us to write our code
 * Return: 0 (Success)
 */

int main(void)
{
	char h;

	int a;

	h = 'a';
	a = '0';
while
(a < 10)
{
	putchar(a);
	a++;
}
while
(h <= 'f')
{
	putchar(h);
	h++;
}
putchar('\n');
return (0);
}

