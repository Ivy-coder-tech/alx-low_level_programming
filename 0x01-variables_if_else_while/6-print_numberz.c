#include <stdio.h>

/**
 * main - Starting point of coding
 * Return: 0 (Finally)
 */

int main(void)
{
	int k;

	k = 0;
	while
		(k < 10)
		{
			putchar(k + '0');
			k++;
		}
	putchar('\n');
	return (0);
}
